# Godot Roguelike 游戏项目文档

## 1. 项目概览

这是一个基于 **Godot 4** 的 2D Roguelike 原型项目。当前代码已经实现了：

- 存档位选择
- 玩家基础属性初始化
- 营地场景与主游戏场景切换
- 随机关卡房间生成
- 多种敌人生成与 AI
- 玩家与敌人的基础伤害结算
- 到达目标后进入下一关
- 玩家死亡后返回营地
- 五种元素魔法系统（火、木、石、冰、雷）
- 玩家闪避机制（闪避时无敌）

当前项目更偏向"玩法框架原型"，核心循环已经成型，但数值系统、Buff 扩展、UI 展示和更多敌人/房间内容仍在继续完善。

## 2. 项目运行流程

### 2.1 启动入口

- 主场景配置在 `project.godot`
- 启动场景是 `scenes/main/select_saves.tscn`

游戏启动后的流程如下：

1. 进入存档选择界面
2. 选择一个存档位
3. 读取或创建 `PlayerProfile`
4. 进入营地场景 `camp.tscn`
5. 在营地到达目标点后进入主游戏场景 `game_loop.tscn`
6. 主游戏中生成地图、房间与敌人
7. 玩家进入终点房间目标点后进入下一关
8. 玩家死亡后退出当前关卡并返回营地

## 3. 核心代码逻辑

### 3.1 存档系统

相关文件：

- `scripts/gui/saves.gd`
- `scripts/player/profile.gd`
- `scenes/gui/saves.tscn`
- `scenes/main/select_saves.tscn`

逻辑说明：

- `saves.gd` 提供 3 个存档位按钮。
- 选择存档位时，会在 `res://saves/` 下查找对应文件，例如 `save1`。
- 如果文件存在，则读取玩家档案。
- 如果文件不存在，则创建一个默认档案并保存。
- 选中后通过 `profile_selected(profile)` 信号把 `PlayerProfile` 传递给主流程场景。

`PlayerProfile` 当前保存的是"永久属性基线"，包括：

- `max_hp`
- `move_speed`
- `jump_height`
- `crit_rate`
- `damage_reduction`

### 3.2 场景切换主线

相关文件：

- `scripts/main/select_saves.gd`
- `scripts/main/camp.gd`
- `scripts/main/game_loop.gd`

逻辑关系：

- `select_saves.gd`：接收存档选择结果，实例化营地场景并传入 `profile`
- `camp.gd`：初始化玩家，等待休息房目标触发后进入主游戏
- `game_loop.gd`：初始化战斗内玩家数据、生成地图和敌人，负责关卡重开与死亡返回

这里的设计思路是：

- `PlayerProfile` 用于保存长期属性
- `PlayerStats` 用于单局战斗中的实时属性和血量
- `PlayerBuffs` 用于战斗过程中对属性进行临时修正

### 3.3 玩家系统

相关文件：

- `scripts/player/player.gd`
- `scripts/player/state.gd`
- `scripts/player/buff.gd`
- `scripts/player/base_magic.gd`
- `scripts/player/fire_magic.gd`
- `scripts/player/wood_magic.gd`
- `scripts/player/stone_magic.gd`
- `scripts/player/ice_magic.gd`
- `scripts/player/lightning_magic.gd`
- `scenes/player/Player.tscn`

#### 玩家节点职责

`Player.tscn` 是一个 `CharacterBody2D`，包含：

- 玩家动画 `AnimatedSprite2D`
- 站立碰撞体 `StandingCollisionShape2D`
- 闪避碰撞体 `SlideCollisionShape2D`
- 攻击轴心 `AttackPivot`（包含攻击判定区）
- 摄像机 `Camera2D`

并且被加入了 `player` 分组，供敌人 AI 搜索目标。

#### 玩家状态管理

`PlayerStats` 负责维护单局中的实时数值：

- 最大生命
- 当前生命
- 移动速度
- 跳跃高度
- 暴击率
- 减伤
- 玩家造成的伤害数组 `damages`
- 玩家受到的伤害数组 `receiving_damages`
- 命中的敌人列表 `enemies`
- 受击无敌帧计时器 `i_frame_timer`
- 闪避计时器 `slide_timer`（便于 Buff 处理）
- 魔法攻击速度 `magic_attack_speed`
- 魔法穿透次数 `magic_pierce_extra`
- 魔法射程加成 `magic_range_extra`

其中伤害数组长度为 6，对应六种伤害类型：

| 索引 | 类型 | 说明 |
|------|------|------|
| 0 | 物理伤害 | 近战攻击 |
| 1 | 火焰伤害 | 火魔法 |
| 2 | 木系伤害 | 木魔法 |
| 3 | 石系伤害 | 石魔法 |
| 4 | 冰系伤害 | 冰魔法 |
| 5 | 雷电伤害 | 雷魔法 |

#### 玩家每帧流程

`player.gd` 在 `_physics_process(delta)` 中的大致顺序为：

1. 应用 Buff 到当前属性
2. 结算本帧攻击与受伤
3. 若生命值小于等于 0，发出 `die` 信号
4. 读取输入，处理近战攻击、远程魔法、闪避
5. 移动处理（走动/滑行）
6. 跳跃处理
7. 调用 `move_and_slide()`
8. 更新动画
9. 根据无敌帧闪烁
10. 调用 `stats.reset(profile)`，为下一帧重新回到基础值

这意味着当前属性系统采用的是"每帧重置基础值，再叠加 Buff，再结算"的模式。

#### 玩家战斗系统

**近战攻击**：
- 按键触发攻击动画
- 通过 `AttackPivot/AttackHitbox` 检测敌人
- 攻击伤害写入 `stats.damages[PHYSIC_ATTACK]`

**远程魔法**：
- 支持五种魔法：火、木、石、冰、雷
- 通过数字键 1-5 切换魔法类型
- 点击鼠标发射对应魔法
- 魔法有冷却时间，受 `magic_attack_speed` 影响

**闪避机制**：
- 按键触发滑行闪避
- 闪避期间使用独立碰撞体
- 闪避时玩家不会受到敌人攻击（无敌）
- 闪避时长由 `slide_duration` 和 `stats.slide_timer` 控制
- 使用 `slide_timer` 方案而非 `animation_finished`，便于 Buff 系统修改闪避时长

#### Buff 系统

`PlayerBuffs` 当前实现了多类 Buff，主要影响：

- 生命上限
- 移动速度
- 跳跃高度
- 减伤
- 暴击率
- 火焰伤害与爆燃效果
- 石系击退与眩晕
- 冰系减速与冻结
- 雷电伤害与连锁

实现方式是：

- `buffs` 数组记录每类 Buff 的层数
- `apply(stats, delta)` 按层数修改当帧属性

### 3.4 敌人系统

相关文件：

- `scripts/enemies/base_enemy.gd`
- `scripts/enemies/state.gd`
- `scripts/enemies/buff.gd`
- `scripts/enemies/enemy_00.gd`
- `scripts/enemies/enemy_01.gd`
- `scripts/enemies/enemy_02.gd`
- `scripts/enemies/enemy_03.gd`
- `scripts/enemies/enemy_generator.gd`

#### 敌人基类

`BaseEnemy` 是敌人的公共父类，负责：

- 初始化属性
- 创建 `EnemyStats` 和 `EnemyBuffs`
- 每帧执行 Buff、伤害结算、AI、动画和死亡判断
- 自动处理死亡和受伤逻辑

其中以下函数要求子类实现：

- `initialize_stats()`
- `handle_ai()`
- `update_animation()`

受伤处理机制：
- `is_getting_attack()` 检测是否受到伤害
- `is_hurt` 标记敌人是否处于硬直状态
- `is_hurt` 由 `animation_finished` 事件重置（而非计时器）
- 子类可重写 `is_getting_attack()` 实现不吃硬直的敌人

#### 敌人状态结构

`EnemyStats` 与玩家状态结构类似，包含：

- 最大生命和当前生命
- 移动速度
- 跳跃高度
- 暴击率
- 减伤（数组，对应六种伤害类型）
- 敌人对玩家造成的伤害数组 `base_damages`
- 敌人受到的伤害数组 `receiving_damages`
- 当前命中的玩家引用 `player`
- 无敌帧时间

#### 敌人种类

当前项目包含四种敌人：

| 敌人 | 名称 | HP | 移动速度 | 攻击范围 | 攻击冷却 | 特点 |
|------|------|----|----------|----------|----------|------|
| enemy_00 | 死灵法师 | 80 | 60 | 150 | 1.5s | 远程攻击，带火焰伤害 |
| enemy_01 | 蘑菇 | 60 | 120 | 80 | 0.8s | 高机动，快速攻击 |
| enemy_02 | 魔古人 | 200 | 40 | 150 | 2.0s | 高防御，石属性攻击 |
| enemy_03 | 死亡使者(Boss) | 500 | 50 | 180 | 1.2s | 多属性攻击，高血量 |

**敌人行为模式**：
- 在检测范围内追踪玩家
- 接近后进入攻击动画（需等待攻击冷却）
- 攻击区域碰到玩家时，对玩家写入伤害
- 攻击前会用射线检测玩家与敌人之间是否被墙体阻挡
- 死亡后播放死亡动画并销毁

**攻击冷却机制**：
- 每个敌人有独立的 `attack_cooldown_timer` 和 `attack_cooldown_duration`
- AI 逻辑中检查冷却状态，防止秒触发攻击

**击退机制**：
- 敌人受到石魔法攻击时会被击退
- 击退由 `EnemyBuffs` 处理，通过 `knock_back` 信号触发
- 击退期间敌人无法行动

#### 敌人生成器

`enemy_generator.gd` 的职责是：

- 读取房间中所有敌人生成点
- 清除上一关残留敌人
- 预加载敌人模板
- 随机选择生成点并实例化敌人

目前：

- 敌人模板目录为 `res://scenes/enemies/`
- 加载多种敌人模板：`enemy_00.tscn`、`enemy_01.tscn`、`enemy_02.tscn`、`enemy_03.tscn`
- 单关最多生成 `10` 个敌人

### 3.5 地图与房间生成系统

相关文件：

- `scripts/rooms/map_generator.gd`
- `scripts/rooms/room_data.gd`
- `scripts/rooms/prefabs/base_room.gd`
- `scripts/rooms/prefabs/start_room.gd`
- `scripts/rooms/prefabs/end_room.gd`
- `scripts/rooms/prefabs/room_00.gd`
- `scripts/rooms/prefabs/room_01.gd`
- `scripts/rooms/prefabs/room_02.gd`
- `scenes/rooms/map_generator.tscn`

#### 房间数据结构

`RoomData` 用于描述一个房间在地图生成中的抽象信息，包括：

- 左上角坐标 `position`
- 房间宽高 `width`、`height`
- 上下左右开口位置
- 对应方向是否已经连接

这里的房间坐标不是像素坐标，而是按网格单位记录。

#### 地图生成思路

`MapGenerator.generate_level()` 的流程如下：

1. 清空上一关
2. 加载普通房间模板
3. 固定放置起始房间 `StartRoom`
4. 从已放置房间的"未使用开口"中随机挑选连接点
5. 从普通房间模板中找到能匹配该方向开口的房间
6. 计算新房间位置
7. 检查是否与已有房间重叠
8. 如果合法，则加入地图并记录连接状态
9. 在达到最少房间数后，尝试放置终点房间 `EndRoom`
10. 所有房间放置完成后，统一更新开口显示状态

如果本轮生成失败，则会清空并重新生成，直到成功为止。

#### 房间基类 BaseRoom

`BaseRoom` 提供公共能力：

- 读取四个方向的开口节点
- 在 `_ready()` 时默认关闭全部开口
- 在 `apply_room_data()` 中根据连接状态打开对应开口
- 在 `get_enemy_points()` 中收集房间内所有敌人生成点

#### 特殊房间

- `start_room.gd`：起始房间，只提供向右出口
- `end_room.gd`：终点房间，带 `Target`，玩家到达后发出 `next_level`
- `rest_room.gd`：营地休息房，到达目标点后发出 `start_game`

#### 普通房间

当前普通房间模板有多种：

- `room_00`
- `room_01`
- `room_02`
- `room_03`
- `room_04`
- `room_05`

它们通过不同的宽高和开口组合来影响随机地图结构。

### 3.6 房间组件

相关文件：

- `scripts/rooms/components/door.gd`
- `scripts/rooms/components/opening.gd`
- `scripts/rooms/components/target.gd`
- `scripts/rooms/components/enemy_point.gd`
- `scripts/rooms/components/wall.tscn`

作用分别为：

- `door.gd`：带碰撞的门，检测到附近有角色时自动开门，无人时关门
- `opening.gd`：静态开口组件，根据连接状态显示开/关
- `target.gd`：目标触发区，玩家进入后发出信号
- `enemy_point.gd`：敌人出生点标记
- `wall.tscn`：墙体组件

## 4. 主要场景结构

### 4.1 主流程场景

- `scenes/main/select_saves.tscn`
  - 游戏启动入口
  - 挂载 `scripts/main/select_saves.gd`
  - 包含存档 UI `scenes/gui/saves.tscn`

- `scenes/main/camp.tscn`
  - 营地/休整场景
  - 挂载 `scripts/main/camp.gd`
  - 包含一个 `RestRoom` 和一个 `Player`

- `scenes/main/game_loop.tscn`
  - 核心游戏场景
  - 挂载 `scripts/main/game_loop.gd`
  - 包含 `MapGenerator`、`EnemyGenerator`、`Player`

### 4.2 角色与敌人场景

- `scenes/player/Player.tscn`
  - 玩家角色场景

- `scenes/enemies/enemy_00.tscn`
  - 死灵法师敌人模板

- `scenes/enemies/enemy_01.tscn`
  - 蘑菇敌人模板

- `scenes/enemies/enemy_02.tscn`
  - 魔古人敌人模板

- `scenes/enemies/enemy_03.tscn`
  - 死亡使者(Boss)敌人模板

- `scenes/enemies/enemy_generator.tscn`
  - 敌人生成器场景

### 4.3 魔法场景

- `scenes/player/fire_magic.tscn`
  - 火魔法投射物

- `scenes/player/wood_magic.tscn`
  - 木魔法投射物

- `scenes/player/stone_magic.tscn`
  - 石魔法投射物（带击退）

- `scenes/player/ice_magic.tscn`
  - 冰魔法投射物

- `scenes/player/lightning_magic.tscn`
  - 雷魔法投射物

- `scenes/player/chain_lightning.tscn`
  - 连锁闪电效果

- `scenes/player/fire_explosion.tscn`
  - 火焰爆燃效果

### 4.4 房间场景

- `scenes/rooms/map_generator.tscn`
  - 地图生成器场景，内置起点房间和终点房间

- `scenes/rooms/prefabs/`
  - `start_room.tscn`
  - `end_room.tscn`
  - `rest_room.tscn`
  - `room_00.tscn`
  - `room_01.tscn`
  - `room_02.tscn`
  - `room_03.tscn`
  - `room_04.tscn`
  - `room_05.tscn`

- `scenes/rooms/component/`
  - 门、开口、目标点、敌人生成点、墙体等基础组件

## 5. 项目文件结构

下面是当前项目的核心目录结构：

```text
godot_game/
├─ assets/
│  ├─ enemy/
│  │  ├─ enemy_00/
│  │  ├─ enemy_01/
│  │  ├─ enemy_02/
│  │  └─ enemy_03/
│  ├─ magic/
│  └─ player/
│     ├─ attack/
│     ├─ idle/
│     ├─ jump/
│     ├─ run/
│     └─ slide/
├─ saves/
│  └─ save1
├─ scenes/
│  ├─ enemies/
│  ├─ gui/
│  ├─ main/
│  ├─ player/
│  └─ rooms/
│     ├─ component/
│     └─ prefabs/
├─ scripts/
│  ├─ enemies/
│  ├─ gui/
│  ├─ main/
│  ├─ player/
│  └─ rooms/
│     ├─ components/
│     └─ prefabs/
└─ project.godot
```

按职责进一步拆分如下：

### 5.1 `scripts/main/`

- `select_saves.gd`：存档选择后的主入口控制
- `camp.gd`：营地逻辑
- `game_loop.gd`：主关卡循环逻辑
- `game_manager.gd`：全局游戏管理器

### 5.2 `scripts/player/`

- `player.gd`：玩家移动、动画、攻击、魔法、闪避与每帧战斗逻辑
- `state.gd`：玩家实时数值结构 `PlayerStats`
- `profile.gd`：玩家存档结构 `PlayerProfile`
- `buff.gd`：玩家 Buff 系统 `PlayerBuffs`
- `base_magic.gd`：魔法基类
- `fire_magic.gd`：火魔法实现
- `wood_magic.gd`：木魔法实现
- `stone_magic.gd`：石魔法实现
- `ice_magic.gd`：冰魔法实现
- `lightning_magic.gd`：雷魔法实现
- `chain_lightning.gd`：连锁闪电实现
- `fire_explosion.gd`：火焰爆燃实现

### 5.3 `scripts/enemies/`

- `base_enemy.gd`：敌人基类
- `state.gd`：敌人数值结构 `EnemyStats`
- `buff.gd`：敌人 Buff 系统 `EnemyBuffs`
- `enemy_00.gd`：死灵法师敌人
- `enemy_01.gd`：蘑菇敌人
- `enemy_02.gd`：魔古人敌人
- `enemy_03.gd`：死亡使者(Boss)
- `enemy_generator.gd`：敌人生成器

### 5.4 `scripts/rooms/`

- `map_generator.gd`：随机房间地图生成器
- `room_data.gd`：房间数据结构
- `components/`：门、开口、目标点、敌人生成点
- `prefabs/`：各种房间逻辑脚本

## 6. 当前实现特点

### 6.1 已形成的核心循环

当前项目已经具备一个完整的轻量 Roguelike 循环：

- 选存档
- 进入营地
- 开始关卡
- 随机关卡生成
- 敌人生成与追击
- 通过终点进入下一关
- 死亡后返回营地

### 6.2 当前代码设计特点

- 使用 `PackedScene.instantiate()` 动态切换主场景
- 将"永久档案"和"战斗内状态"分离
- 用数组预留多伤害类型结算结构
- 房间通过开口匹配方式进行拼接
- 房间内敌人出生点由场景标记决定
- 伤害结算集中在 `Area2D` 中处理，不注入 `_physics_process`
- 使用 `slide_timer` 而非 `animation_finished` 处理闪避，便于 Buff 扩展

## 7. 当前已知限制与可扩展方向

从当前代码来看，还存在一些明显的"原型阶段"特征：

- 存档路径使用的是 `res://saves/`，更适合开发阶段；正式发布通常更推荐 `user://`
- Buff 虽已定义较多类型，但获取方式和 UI 展示尚未实现
- 主循环中暂未看到血条、数值面板、关卡信息等 UI
- 房间模板数量可进一步增加，提升地图多样性
- 敌人 AI 行为模式可更加多样化

## 8. 总结

这个项目目前已经完成了一个可运行的 Roguelike 基础框架，重点在于：

- 场景切换流程清晰
- 数据结构已经初步模块化
- 随机关卡生成框架已经成型
- 敌人与玩家的基础战斗结算链路已打通
- 五种元素魔法系统已实现
- 玩家闪避无敌机制已完善
- 敌人攻击冷却机制已添加

如果后续继续开发，比较自然的扩展顺序是：

1. 完善玩家攻击与命中系统
2. 增加敌人种类与房间模板
3. 增加关卡 UI 和属性展示
4. 完善 Buff 获取与成长系统
5. 将存档和局外成长体系做完整